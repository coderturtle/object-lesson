---
import "../../styles/global.css";

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Object Lesson: learn to think in objects the way you already work, harness in hand, and let the compiler and a real test suite, not a quiz, be the first gate.",
} = Astro.props;

const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <header class="site-header">
      <div class="prose-narrative flex items-center justify-between px-4 py-6">
        <a href={base} class="brand-mark font-mono text-sm uppercase tracking-widest no-underline">
          Object Lesson
        </a>
        <nav class="site-nav flex gap-5 text-sm">
          <a href={base}>Guide</a>
          <a href={`${base}build-log/`}>Build log</a>
        </nav>
      </div>
    </header>
    <main class="prose-narrative px-4 pb-16">
      <slot />
    </main>
    <footer class="prose-narrative px-4 py-8 text-sm opacity-70">
      <p>
        The workshop itself lives in the <a href="https://github.com/coderturtle/object-lesson" class="underline">repo</a>, not on this site. This site is the guide and the build-in-public journal; the exercises run in your own coding-agent harness.
      </p>
    </footer>
  </body>
</html>
